package parse

import "fmt"

// Parse reads one XSD file and returns a Schema (not yet resolved).
// This is a stub; full implementation is in Step 2.
func Parse(filename string) (*Schema, error) {
	return nil, fmt.Errorf("parse.Parse: not yet implemented")
}
